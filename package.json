{
  "name": "pi-herdr-auto-reload",
  "version": "1.0.0",
  "description": "Reload every live Pi session managed by the current Herdr server.",
  "type": "module",
  "license": "MIT",
  "author": "bradmatic",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/geekmuse/pi-extensions-herdr-auto-reload.git"
  },
  "bugs": {
    "url": "https://github.com/geekmuse/pi-extensions-herdr-auto-reload/issues"
  },
  "homepage": "https://github.com/geekmuse/pi-extensions-herdr-auto-reload#readme",
  "keywords": [
    "pi-package",
    "pi-extension",
    "herdr"
  ],
  "engines": {
    "node": ">=18"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "pi": {
    "extensions": [
      "./dist/index.js"
    ]
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "devDependencies": {
    "@types/node": "^24.12.4",
    "typescript": "^5.9.3"
  }
}
